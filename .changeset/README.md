# Changesets

This folder stores release notes and version bumps for workspace packages.

- Create a new changeset: `bun run changeset`
- Apply versions: `bun run version-packages`
- Publish packages: `bun run release`
